name: Gradle Check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  gradle-check:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v5
      - name: Verify with Gradle
        run: >-
          xvfb-run -a ./gradlew -g .gradle-user-home
          cleanTest test javadoc jar verifyNoRuntimeDependencies
          --info --no-daemon --stacktrace
      - name: Upload JAR
        uses: actions/upload-artifact@v7
        with:
          name: javif.jar
          path: build/libs/*.jar
